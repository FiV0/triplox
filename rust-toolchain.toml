[toolchain]
# Pinned to a known-good stable. rustc 1.97.0 (2026-07-07) ICEs while
# compiling the `dbsp` dependency (vtable-slot resolution for
# StarJoinFuncTrait / dyn_clone::DynClone). Unpin once a fixed rustc ships.
channel = "1.95.0"
components = ["rustfmt", "clippy"]
